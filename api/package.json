{
  "name": "gtfs-compass-api",
  "private": true,
  "scripts": {
    "migrate:local": "wrangler d1 migrations apply gtfs-compass --local",
    "migrate:remote": "wrangler d1 migrations apply gtfs-compass --remote",
    "build:ui": "npm --prefix ../config-ui run build",
    "pretest": "npm run build:ui",
    "test": "vitest run",
    "generate:proto": "./scripts/generate-proto.sh",
    "predeploy": "npm run build:ui",
    "deploy": "wrangler d1 migrations apply gtfs-compass --remote && wrangler deploy",
    "predev": "npm run build:ui",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@cloudflare/workers-types": "^5.20260801.1",
    "@types/node": "^26.1.2",
    "protobufjs-cli": "^2.6.1",
    "typescript": "^7.0.2",
    "vitest": "4.1.10",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "protobufjs": "^8.7.1"
  },
  "type": "module"
}
